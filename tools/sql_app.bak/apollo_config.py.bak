import os
import sys

HERE = os.path.abspath(__file__)
HOME_DIR = os.path.split(os.path.split(HERE)[0])[0]
os.sys.path.append(HOME_DIR)

appName = "op-robot-feishu-api"

from sql_app import apollo_manage
#from sql_app.robot_manage import appName

DB_HOST = apollo_manage.getApolloConfig("DB_HOST", appName)
DB_USER = apollo_manage.getApolloConfig("DB_USER", appName)
DB_PASSWD = apollo_manage.getApolloConfig("DB_PASSWD", appName)
DB_PORT = apollo_manage.getApolloConfig("DB_PORT", appName)
DB_NAME = apollo_manage.getApolloConfig("DB_NAME", appName)

